# copy this file to .env and fill in your own values

PORT=5000

# MongoDB Atlas connection string (added in the next step)
MONGO_URI=

# secret used to sign login tokens (added with auth)
JWT_SECRET=
